# 🚀 Локальный запуск бота MainerCrypto

## Быстрый старт

### Вариант 1: Автоматическая установка (Windows)

1. Запустите `setup_local.bat`
2. Следуйте инструкциям на экране
3. Бот запустится автоматически

### Вариант 2: Ручная установка

#### 1. Установите Python 3.9+

Скачайте с [python.org](https://www.python.org/downloads/)

#### 2. Создайте виртуальное окружение

```bash
python -m venv venv
```

#### 3. Активируйте виртуальное окружение

**Windows:**
```bash
venv\Scripts\activate
```

**Linux/Mac:**
```bash
source venv/bin/activate
```

#### 4. Установите зависимости

```bash
pip install -r req.txt
```

#### 5. Создайте файл `.env`

Создайте файл `.env` в корне проекта со следующим содержимым:

```env
# Токен бота от @BotFather
BOT_TOKEN=ваш_токен_бота_здесь

# ID администраторов через запятую
ADMIN_IDS=6177558353

# Для локального запуска используйте SQLite (рекомендуется)
DATABASE_URL=sqlite+aiosqlite:///./mainercrypto.db
```

**Или для PostgreSQL:**

```env
BOT_TOKEN=ваш_токен_бота_здесь
ADMIN_IDS=6177558353
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_HOST=localhost
POSTGRES_NAME=mainercrypto
```

#### 6. Запустите бота

```bash
python main.py
```

## 📝 Получение токена бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям
4. Скопируйте полученный токен в файл `.env`

## 🗄️ База данных

### SQLite (рекомендуется для локального запуска)

- Не требует установки дополнительного ПО
- База данных создается автоматически в файле `mainercrypto.db`
- Готова к использованию сразу после запуска

### PostgreSQL (для продакшена)

Если хотите использовать PostgreSQL локально:

1. **Установите PostgreSQL:**
   - Windows: [Скачать PostgreSQL](https://www.postgresql.org/download/windows/)
   - Или используйте Docker: `docker run --name postgres -e POSTGRES_PASSWORD=postgres -p 5432:5432 -d postgres`

2. **Создайте базу данных:**
   ```sql
   CREATE DATABASE mainercrypto;
   ```

3. **Настройте .env файл** с параметрами PostgreSQL

## ✅ Проверка работы

После запуска бота:

1. Откройте Telegram
2. Найдите вашего бота по имени
3. Отправьте команду `/start`
4. Должно появиться приветственное сообщение

## 🔧 Решение проблем

### Ошибка "BOT_TOKEN not set"
- Проверьте, что файл `.env` существует и содержит `BOT_TOKEN`

### Ошибка подключения к базе данных
- Для SQLite: убедитесь, что у вас есть права на запись в директорию проекта
- Для PostgreSQL: проверьте, что PostgreSQL запущен и настройки в `.env` правильные

### Ошибка импорта модулей
- Убедитесь, что виртуальное окружение активировано
- Переустановите зависимости: `pip install -r req.txt`

## 📁 Структура после запуска

После первого запуска создадутся:
- `mainercrypto.db` - база данных SQLite (если используется)
- Таблицы в базе данных автоматически
- Начальные данные (монеты, алгоритмы) добавятся автоматически

## 🛑 Остановка бота

Нажмите `Ctrl+C` в терминале, где запущен бот

